<Page x:Class="Finances.Pages.Credit.CreditScorePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Style="{StaticResource PageStyle}"
      Loaded="CreditScorePage_Loaded"
      Title="Credit Scores">
    <Grid x:Name="Grid" Margin="0,0,0,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ListView x:Name="LVScores" Grid.Row="0" Height="450" SelectionChanged="LVScores_SelectionChanged" SelectionMode="Single" ScrollViewer.VerticalScrollBarVisibility="Visible" AlternationCount="2" Margin="0,0,0,10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="130" DisplayMemberBinding="{Binding Path=DateToString}">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Tag="Date" Click="LVScoresColumnHeader_Click" Content="Date" />
                        </GridViewColumn.Header>
                    </GridViewColumn>
                    <GridViewColumn Width="250" DisplayMemberBinding="{Binding Path=Source}">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Tag="Source" Click="LVScoresColumnHeader_Click" Content="Source" />
                        </GridViewColumn.Header>
                    </GridViewColumn>
                    <GridViewColumn Width="120" DisplayMemberBinding="{Binding Path=Score}">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Tag="Score" Click="LVScoresColumnHeader_Click" Content="Score" />
                        </GridViewColumn.Header>
                    </GridViewColumn>
                    <GridViewColumn Width="150" DisplayMemberBinding="{Binding Path=ProviderToString}">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Tag="Provider" Click="LVScoresColumnHeader_Click" Content="Provider" />
                        </GridViewColumn.Header>
                    </GridViewColumn>
                    <GridViewColumn Width="120" DisplayMemberBinding="{Binding Path=FICOToString}">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Tag="FICO" Click="LVScoresColumnHeader_Click" Content="FICO?" />
                        </GridViewColumn.Header>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="BtnAddCreditScore" Width="160" Content="_Add Credit Score" Click="BtnAddCreditScore_Click" IsDefault="True" Margin="0,0,10,0" />
            <Button x:Name="BtnDeleteCreditScore" Width="180" Content="_Delete Credit Score" Click="BtnDeleteCreditScore_Click" IsEnabled="False" Margin="0,0,10,0" />
            <Button x:Name="BtnModifyCreditScore" Width="180" Content="_Modify Credit Score" Click="BtnModifyCreditScore_Click" IsEnabled="False" Margin="0,0,10,0" />
            <Button x:Name="BtnBack" Width="125" Content="_Back" Click="BtnBack_Click" IsCancel="True" />
        </StackPanel>
    </Grid>
</Page>