<Page x:Name="PageRenameAccount" x:Class="Finances.Pages.Accounts.RenameAccountPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
      Loaded="RenameAccountPage_OnLoaded"
        Style="{StaticResource PageStyle}"
        Title="Rename Account">
    <Grid x:Name="Grid" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="What name would you like this account to have?" Width="250" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,5" />
        <TextBox x:Name="TxtAccountName" Grid.Row="1" Width="250" Padding="5" Margin="0,0,0,10" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="BtnSubmit" Width="120" Click="BtnSubmit_Click" IsEnabled="False" IsDefault="True" Margin="0,0,10,0">
                <AccessText Text="_Submit" />
            </Button>
            <Button x:Name="BtnCancel" Width="120" Click="BtnCancel_Click" IsCancel="True">
                <AccessText Text="_Cancel" />
            </Button>
        </StackPanel>
    </Grid>
</Page>