<Window x:Class="Finances.Windows.CategoryRenamePopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Finances.Windows"
        mc:Ignorable="d"
        Title="CategoryRenamePopup" Width="Auto" Height="Auto" Closing="Window_Closing"  ResizeMode="NoResize" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen" FontFamily="Segoe UI Semibold" FontSize="17" Icon="Money.ico">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="txtName" Grid.Column="0" Grid.Row="0" Width="300" Margin="0,0,0,5" Grid.ColumnSpan="2" TextChanged="txtName_TextChanged"/>
        <Button x:Name="btnSubmit" Grid.Column="0" Content="_Submit"  Grid.Row="1" Margin="0,0,10,5"  Width="Auto" IsDefault="True" IsEnabled="False" Click="btnSubmit_Click"/>
        <Button x:Name="btnCancel" Grid.Column="1" Content="_Cancel"  Grid.Row="1" Margin="0,0,0,5" Width="Auto"/>
    </Grid>
</Window>
